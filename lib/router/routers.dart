import 'package:crypto_coin/feature/body_app.dart';

import '../widgets/nested_screens/nested_first.dart';

final routes = {
  '/': (context) => const BuildingApp(),
  '/first': (context) => const NestedFirstScreen(),
};
